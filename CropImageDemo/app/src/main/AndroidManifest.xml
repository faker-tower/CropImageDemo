<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.axiang.cropimagedemo">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CropImageDemo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CropImageDemo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".selectpic.SelectPicActivity"
            android:exported="false"
            android:label="@string/title_activity_crop"
            android:theme="@style/Theme.CropImageDemo.NoActionBar" />
        <activity
            android:name=".editimg.EditImageActivity"
            android:exported="false"
            android:label="@string/title_activity_edit_image"
            android:theme="@style/Theme.CropImageDemo.NoActionBar" />
    </application>

</manifest>